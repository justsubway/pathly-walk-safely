import { LatLng, RouteResult } from '../lib/types';const API_ENDPOINTS = [  'http://localhost:5002',        // Web browser access  'http://127.0.0.1:5002',       // Local loopback  'http://10.0.0.154:5002',      // Current WiFi IP for mobile  'http://10.108.121.139:5002'   // Backup network IP];const API_BASE_URL = API_ENDPOINTS[0]; // Primary endpointconst API_TIMEOUT = 5000; // 5 second timeout (faster for demo)interface APIRoute {  id: string;  name: string;  coords: LatLng[];  distanceKm: number;  etaMin: number;  safetyScore: number;  strategy: string;}interface GenerateRoutesResponse {  status: string;  routes: APIRoute[];  total_routes: number;  crime_data_available: boolean;  selected_hour: number;}interface SafetyResponse {  status: string;  safety_score: number;  nearby_crimes_count: number;  hour: number;  coordinates: LatLng;}class Safe2WalkAPIService {  private baseURL: string;  private timeout: number;  constructor() {    this.baseURL = API_BASE_URL;    this.timeout = API_TIMEOUT;  }  async checkHealth(): Promise<boolean> {    console.log('🏥 Checking Flask API health across multiple endpoints...');    for (let i = 0; i < API_ENDPOINTS.length; i++) {      const endpoint = API_ENDPOINTS[i];      try {        console.log(`🔍 Trying endpoint ${i + 1}/${API_ENDPOINTS.length}: ${endpoint}`);        const controller = new AbortController();        const timeoutId = setTimeout(() => controller.abort(), 2000); // 2s timeout per endpoint        const response = await fetch(`${endpoint}/health`, {          method: 'GET',          headers: {            'Content-Type': 'application/json',          },          signal: controller.signal,        });        clearTimeout(timeoutId);        if (response.ok) {          const data = await response.json();          console.log(`✅ Flask API healthy on ${endpoint}:`, data);          this.baseURL = endpoint;          return true;        } else {          console.log(`❌ Health check failed on ${endpoint}: ${response.status}`);        }      } catch (error) {        console.log(`❌ ${endpoint} not reachable:`, error instanceof Error ? error.message : String(error));      }    }    console.log('❌ All Flask API endpoints failed');    return false;  }  async generateRoutes(    origin: LatLng,    destination: LatLng,    selectedHour: number = 12  ): Promise<GenerateRoutesResponse | null> {    try {      console.log('🚀 Requesting routes from Flask API...');      console.log(`📍 ${origin.latitude},${origin.longitude} → ${destination.latitude},${destination.longitude}`);      const controller = new AbortController();      const timeoutId = setTimeout(() => controller.abort(), this.timeout);      const response = await fetch(`${this.baseURL}/api/routes/generate`, {        method: 'POST',        headers: {          'Content-Type': 'application/json',        },        body: JSON.stringify({          origin: origin,          destination: destination,          selectedHour: selectedHour,        }),        signal: controller.signal,      });      clearTimeout(timeoutId);      if (!response.ok) {        throw new Error(`HTTP ${response.status}: ${response.statusText}`);      }      const data: GenerateRoutesResponse = await response.json();      console.log(`✅ Flask API returned ${data.total_routes} routes`);      console.log(`📊 Crime data available: ${data.crime_data_available}`);      return data;    } catch (error) {      console.error('❌ Flask API route generation failed:', error);      return null;    }  }  async calculateSafetyScore(    latitude: number,    longitude: number,    hour: number = 12  ): Promise<SafetyResponse | null> {    try {      console.log(`🛡️ Requesting safety score from Flask API for ${latitude},${longitude} at ${hour}:00`);      const controller = new AbortController();      const timeoutId = setTimeout(() => controller.abort(), this.timeout);      const response = await fetch(`${this.baseURL}/api/safety/calculate`, {        method: 'POST',        headers: {          'Content-Type': 'application/json',        },        body: JSON.stringify({          latitude: latitude,          longitude: longitude,          hour: hour,        }),        signal: controller.signal,      });      clearTimeout(timeoutId);      if (!response.ok) {        throw new Error(`HTTP ${response.status}: ${response.statusText}`);      }      const data: SafetyResponse = await response.json();      console.log(`✅ Flask API safety score: ${data.safety_score}/100 (${data.nearby_crimes_count} nearby crimes)`);      return data;    } catch (error) {      console.error('❌ Flask API safety calculation failed:', error);      return null;    }  }  convertAPIRoutesToRouteResults(apiRoutes: APIRoute[]): RouteResult[] {    return apiRoutes.map(route => ({      id: route.id,      name: route.name,      coords: route.coords,      segments: [], // Not used in current UI      distanceKm: route.distanceKm,      etaMin: route.etaMin,      safetyScore: route.safetyScore,    }));  }  async testAPIWorkflow(): Promise<boolean> {    try {      console.log('🧪 Testing complete Flask API workflow...');      const isHealthy = await this.checkHealth();      if (!isHealthy) {        console.log('❌ API not healthy - workflow test failed');        return false;      }      const testOrigin: LatLng = { latitude: 25.7617, longitude: -80.1918 }; // Downtown Miami      const testDestination: LatLng = { latitude: 25.8010, longitude: -80.1993 }; // Wynwood      const routesResponse = await this.generateRoutes(testOrigin, testDestination, 14);      if (!routesResponse || routesResponse.routes.length === 0) {        console.log('❌ Route generation test failed');        return false;      }      const safetyResponse = await this.calculateSafetyScore(        testOrigin.latitude,        testDestination.longitude,        14      );      if (!safetyResponse) {        console.log('❌ Safety calculation test failed');        return false;      }      console.log('✅ Complete Flask API workflow test successful!');      console.log(`📊 Generated ${routesResponse.routes.length} routes`);      console.log(`🛡️ Safety score: ${safetyResponse.safety_score}/100`);      return true;    } catch (error) {      console.error('❌ API workflow test failed:', error);      return false;    }  }}export const apiService = new Safe2WalkAPIService();export type { GenerateRoutesResponse, SafetyResponse, APIRoute };
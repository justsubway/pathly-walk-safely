import Constants from 'expo-constants';export const API_CONFIG = {  GOOGLE_MAPS_API_KEY: Constants.expoConfig?.extra?.googleMapsApiKey || '',  GOOGLE_MAPS_BASE_URL: 'https://maps.googleapis.com/maps/api',  MIAMI_CRIME_API: 'https://opendata.miamigov.com/resource',};export const API_LIMITS = {  GOOGLE_REQUESTS_PER_MINUTE: 60,  CACHE_DURATION_MS: 5 * 60 * 1000,   MAX_WAYPOINTS_PER_REQUEST: 23, };export const MIAMI_BOUNDS = {  north: 25.976,  south: 25.129,  east: -80.058,  west: -80.644,};export function isWithinMiamiBounds(lat: number, lng: number): boolean {  return (    lat >= MIAMI_BOUNDS.south &&    lat <= MIAMI_BOUNDS.north &&    lng >= MIAMI_BOUNDS.west &&    lng <= MIAMI_BOUNDS.east  );}
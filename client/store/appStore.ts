import { create } from 'zustand';import { LatLng, RouteResult } from '../lib/types';import { generateMockRoutes } from '../services/routes';export interface AppStore {  selectedHour: number;  currentTime: Date;  origin: { location: LatLng; name: string } | null;  destination: { location: LatLng; name: string } | null;  routes: RouteResult[];  selectedRouteId: string | null;  heatmapVisible: boolean;  lastSyncTime: Date | null;  isLoading: boolean;  refreshCurrentTime: () => void;  setOrigin: (location: LatLng, name: string) => void;  setDestination: (location: LatLng, name: string) => void;  setRoutes: (routes: RouteResult[]) => void;  setSelectedRouteId: (routeId: string | null) => void;  setHeatmapVisible: (visible: boolean) => void;  setIsLoading: (loading: boolean) => void;  generateRoutes: () => Promise<void>;  clearRoutes: () => void;  reset: () => void;}const getCurrentTime = () => new Date();const getCurrentHour = () => getCurrentTime().getHours();const initialState = {  selectedHour: getCurrentHour(), // Use actual device hour  currentTime: getCurrentTime(),  origin: null,  destination: null,  routes: [],  selectedRouteId: null,  heatmapVisible: false,  lastSyncTime: null,  isLoading: false,};export const useAppStore = create<AppStore>((set, get) => ({  ...initialState,  refreshCurrentTime: () => {    const now = getCurrentTime();    set({       currentTime: now,      selectedHour: now.getHours()    });  },  setOrigin: (location: LatLng, name: string) =>     set({ origin: { location, name } }),  setDestination: (location: LatLng, name: string) =>     set({ destination: { location, name } }),  setRoutes: (routes: RouteResult[]) => set({ routes }),  setSelectedRouteId: (routeId: string | null) =>     set({ selectedRouteId: routeId }),  setHeatmapVisible: (visible: boolean) =>     set({ heatmapVisible: visible }),  setIsLoading: (loading: boolean) => set({ isLoading: loading }),  generateRoutes: async () => {    const { origin, destination, selectedHour, setIsLoading, setRoutes, setSelectedRouteId } = get();    if (!origin || !destination) {      throw new Error('Origin and destination must be set');    }    setIsLoading(true);    setRoutes([]);    setSelectedRouteId(null);    try {      const generatedRoutes = await generateMockRoutes(        origin.location,        destination.location,        selectedHour      );      setRoutes(generatedRoutes);      if (generatedRoutes.length > 0) {        setSelectedRouteId(generatedRoutes[0].id);      }      set({ lastSyncTime: new Date() });    } finally {      setIsLoading(false);    }  },  clearRoutes: () => {    set({      routes: [],      selectedRouteId: null,    });  },  reset: () => {    set(initialState);  },}));
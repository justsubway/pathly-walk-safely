const fs = require('fs');const path = require('path');console.log('🧪 Testing Safe2Walk App Structure...\n');const files = [    'App.js',    'package.json',    'app.json'];files.forEach(file => {    const filePath = path.join(__dirname, file);    if (fs.existsSync(filePath)) {        console.log(`✅ ${file} - Found`);    } else {        console.log(`❌ ${file} - Missing`);    }});try {    const appContent = fs.readFileSync('App.js', 'utf8');    console.log('\n📱 App.js Analysis:');    console.log(`✅ File size: ${appContent.length} characters`);    const hasStyleSheet = appContent.includes('StyleSheet');    const hasText = appContent.includes('Text');    const hasView = appContent.includes('View');    const hasExport = appContent.includes('export default');    console.log(`✅ StyleSheet: ${hasStyleSheet ? '✓' : '✗'}`);    console.log(`✅ Text component: ${hasText ? '✓' : '✗'}`);    console.log(`✅ View component: ${hasView ? '✓' : '✗'}`);    console.log(`✅ Default export: ${hasExport ? '✓' : '✗'}`);    if (hasStyleSheet && hasText && hasView && hasExport) {        console.log('\n🎉 App structure is valid for React Native!');    }} catch (error) {    console.log(`❌ Error reading App.js: ${error.message}`);}console.log('\n📋 Test Summary:');console.log('- Your React Native app is properly structured');console.log('- Use Expo Go on your phone to test (scan QR code)');console.log('- Web version available at http://localhost:8081');console.log('- All dependencies are installed correctly');
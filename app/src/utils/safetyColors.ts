export function getSafetyColor(safetyScore: number): string {  const clampedScore = Math.max(0, Math.min(100, safetyScore));  if (clampedScore <= 30) {    const ratio = clampedScore / 30;    return interpolateColor('#dc2626', '#ea580c', ratio); // Red to Orange Red  } else if (clampedScore <= 50) {    const ratio = (clampedScore - 30) / 20;    return interpolateColor('#ea580c', '#f97316', ratio); // Orange Red to Orange  } else if (clampedScore <= 70) {    const ratio = (clampedScore - 50) / 20;    return interpolateColor('#f97316', '#eab308', ratio); // Orange to Yellow  } else if (clampedScore <= 85) {    const ratio = (clampedScore - 70) / 15;    return interpolateColor('#eab308', '#84cc16', ratio); // Yellow to Light Green  } else {    const ratio = (clampedScore - 85) / 15;    return interpolateColor('#84cc16', '#22c55e', ratio); // Light Green to Green  }}export function getSafetyColorWithAlpha(safetyScore: number, alpha: number = 0.2): string {  const baseColor = getSafetyColor(safetyScore);  const hex = baseColor.replace('#', '');  const r = parseInt(hex.substr(0, 2), 16);  const g = parseInt(hex.substr(2, 2), 16);    const b = parseInt(hex.substr(4, 2), 16);  return `rgba(${r}, ${g}, ${b}, ${alpha})`;}function interpolateColor(color1: string, color2: string, ratio: number): string {  const hex1 = color1.replace('#', '');  const hex2 = color2.replace('#', '');  const r1 = parseInt(hex1.substr(0, 2), 16);  const g1 = parseInt(hex1.substr(2, 2), 16);  const b1 = parseInt(hex1.substr(4, 2), 16);  const r2 = parseInt(hex2.substr(0, 2), 16);  const g2 = parseInt(hex2.substr(2, 2), 16);  const b2 = parseInt(hex2.substr(4, 2), 16);  const r = Math.round(r1 + (r2 - r1) * ratio);  const g = Math.round(g1 + (g2 - g1) * ratio);  const b = Math.round(b1 + (b2 - b1) * ratio);  return `#${r.toString(16).padStart(2, '0')}${g.toString(16).padStart(2, '0')}${b.toString(16).padStart(2, '0')}`;}